# Absent-execution bank selector: development analysis card

**Status: post-hoc development analysis, not a prospective method experiment.**
A5 outcomes have already been inspected. Do not call a later rule evaluation blind.

Question: within the existing two/three-second hold pool, how much headroom remains
for selection, and can paid obstacle-absent execution identify it? This is a stronger
baseline for a future repair comparison, not a new motion generator or repair claim.

Use all eight A5 units, default mode, fixed2/fixed3 and four matched repeats. For each
unit choose one hold for all repeats from **absent** execution only, lexicographically:
virtual target-beam local pass count, count of observed4 s horizons, median4 s root x
only if all four horizons are observed, then fixed3 on an exact tie. Freeze this rule
before computing this analysis. Missing suffixes are not zero errors or omitted survivors.

Read present outcomes only after choosing. Report32 assigned slots/eight units, gains
and losses against fixed3. Also report the best fixed hold per unit and slotwise union
as explicitly outcome-informed ceilings, not executable policies. All data remains in
its original development group; no statistical generalization or quality claim.

Cost:16 existing references and64 existing absent executions inform the bank. Evaluation
reuses the64 present records; no new GPU work or training. Any future practical comparison
must charge this bank cost and give applicable baselines equal access. If little ceiling
remains, improve candidate supply/transmission rather than train a better selector.
