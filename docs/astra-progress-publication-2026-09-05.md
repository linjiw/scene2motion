# Progress sensitivity and receipt-derived public presentation

## Research follow-up

The preceding recovery audit's strict positive-displacement condition excluded one
essentially stationary fixed2/default pass. A CPU-only follow-up tests whether the
candidate-pool conclusion depends only on that boundary. This is explicitly post hoc:
no threshold is calibrated or recommended, no historical label changes, and no new
motion or simulation is generated.

Keep complete one-second observation and all previously defined kinematic bounds.
Sweep strict net forward displacement; retain eight carrier groups and four matched
slot repeats per hold. The outcome-informed oracle below may choose a different hold
for each slot; it is an upper bound for the recorded pool, not a practical policy.

| Net displacement must exceed | fixed2 /32 | fixed3 /32 | Slotwise oracle /32 |
| --- | ---: | ---: | ---: |
| −1 mm | 14 | 17 | 18 |
| 0 mm | 13 | 17 | 17 |
| 10 mm | 13 | 17 | 17 |
| 50 mm | 13 | 17 | 17 |
| 100 mm | 13 | 17 | 17 |
| 250 mm | 9 | 17 | 17 |
| 500 mm | 3 | 15 | 15 |

The best fixed hold per unit has the same ceiling at every listed threshold. All17
fixed3/default local passes advance more than250 mm afterward;15 exceed500 mm. The
oracle's eighteenth case appears only when tolerating the near-stationary candidate's
−0.313 mm displacement. This favors candidate modification over selector training in
this spent pool, without establishing an execution-quality rule or scene transfer.

Receipt: `outputs/astra_a5_progress_sensitivity_v1/summary.json`. Its source hash is
the original recovery receipt's hash; the analyzer revalidates that audit's dependencies.
Tests cover strict boundaries, missing/duplicate membership, lineage, censored windows,
kinematic violations and nonfinite displacements. A second build matches exactly.

## Public method and results

`docs/site/_progress.html` explains the native root-height intervention, frozen
generator/controller interface and achieved-state scoring. Both the main page and
companion viewer share the same generated block. `docs/site/build_progress.py`
checks source hashes, all-assigned membership and row/count agreement, then emits
the nine-row A5 comparison, seven-row sensitivity table and `docs/progress.json`.
Its dated narrative refuses changed headline outcomes that require prose revision.

The page retains the losing A4 adaptive comparison, null A5 mode comparison and
failed stepping repair. It distinguishes replay contact observations from original
execution labels, and the proposed compiler from the implemented fixed-duck baseline.
Existing videos stay labelled as historical stepping footage; no ducking video or
motion-quality certification is fabricated. The research-writing skill informed these
evidence boundaries, rather than a novelty or acceptance claim.

## Publication boundary

The live site was configured to serve `master:/docs`, while the local research branch
was50 commits ahead with motion payloads and unreleased work. The publication workflow
uses a dedicated `gh-pages:/docs` branch. An explicit exporter reads committed files,
allows static assets, selected result/protocol documents and JSON receipts, and excludes
motion pickle/array/checkpoint payloads, process logs, private paper drafts and the
unrelated ledger. `docs/publication.json` binds every exported file to a source commit.
This is a website update, not a dataset payload release or push of the research history.

Local Chromium checks cover desktop/light, mobile/dark and the companion viewer.
The full CPU regression suite passed1185 tests in211.46 s; the23 focused publication/
sensitivity checks passed, and the scientific sensitivity receipt rebuilt exactly.
Long historical code paths were fixed to wrap at mobile width; the main and companion
pages fit390 px without document overflow. Live publication must additionally verify
the Pages build commit and fetched public snapshot before being reported as complete.

## Resource admission

A resumed180-second invocation of the unchanged contact-event continuation produced
ten more preflight refusals. Available RAM9421–10341 MiB passed the9216 MiB gate;
free VRAM4067–4072 MiB failed the5500 MiB gate. No simulator launched, no scientific
seed was spent, no co-tenant was interrupted and no poller remains. These refusals
are preserved alongside the prior ten, not counted as process launches or trial failures.
Contact-event attribution and a controlled positive G1 probe remain unfinished.
