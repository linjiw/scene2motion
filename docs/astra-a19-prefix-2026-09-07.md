# A19 fresh-scene experiment: verified prefix, then resource abort

A19 generated all32 historical comparator references and verified its nominal
batch and exact replay. Nominal qualified4/16 fresh scene–carrier units under
the unchanged obstacle-absent clearance/exit/complete-continuation criterion.
All32 replay state traces, termination flags, timesteps and encoder signatures
matched. This is useful apparatus and baseline evidence, not joint-control gain.

Two shallow target jobs also completed: the first qualified, the second had no
arrival. The third shallow target job (a19s01u0) was aborted when available RAM
reached2495MiB, below the preregistered2500MiB running floor. It returned no
complete state archive. The failed launch is preserved and was not retried.
Four jobs were verified and the fifth failed:160 operational exposures launched,
128 complete verified rollouts,32 invalid exposures from the aborted job. There
are18 unique scientific measurements (16 nominal, two shallow), plus the replay
and context workload. The full shallow-arm rate is unknown;1/2 is only a prefix.

No joint candidate has been generated or previewed. The176-preview candidate
coverage endpoint, timing-supply gate and present capability bridge are untested.
Do not call this a negative control-mechanism result, fit a predictor, or remove
the aborted assignment from a completed-cohort denominator.

## Implemented technical change

The compiler exposes depth(.26,.28,.30)m and forward timing beta(-.24,0,.24)s.
Nominal achieved body-envelope entry and rear exit determine one frozen spatial
window per unit, with an explicit geometry fallback for missing events. Native
root-position and root-height requests change before ARDY generation; there is
no finished joint-trajectory warp. The historical root endpoints and horizon are
preserved. The exact old nominal and shallow compilers remain separate controls.

A CPU construction audit checks nine distinct joint requests for every assigned
unit, including units without observed nominal events. This establishes distinct
native controls, not distinct generated references or improved achieved motion.
The audit also reconstructs all18 scientific measurements and verifies the
32-slot prepared contexts. See [prefix audit](../outputs/astra_a19_prefix_audit_v1/summary.json),
[frozen protocol](astra-a19-joint-control-protocol.md), and
[paper claim–evidence plan](astra-icra-joint-control-plan-2026-09-07.md).

## Next executable research decision

The frozen attempt cannot be resumed through its spent failed job: the supervisor
correctly refuses retries. Retain preparation /tmp/s2m-a19-b7f9733 and all outputs.
After adequate host resources are available, preregister a replacement allocation
that explicitly accounts for this interrupted attempt and retains the same task,
controls and qualification thresholds. Resource stabilization must precede another
large allocation; do not reduce32 environments or lower the running floor to
force completion. The next scientific question remains useful joint-control supply,
then obstacle-present capability, then efficient scene-held-out correction.
