# A20 conditional present capability bridge (design only)

**Status: design only; no jobs assigned or launched.** A19 must first pass its
preregistered repeated timing-supply gate. This document does not authorize a
larger campaign or alter any spent scene's thresholds.

Question: can useful A19 joint controls produce physical-simulation passage and
complete continuation with the obstacle present? This is candidate capability,
not an equal-budget algorithm comparison. A19's nine-candidate search is charged.

Proposed16 A19 development units, paired three arms: A19's frozen absent-selected
joint candidate, exact fixed shallow, and the established fixed3/default command.
Maximum48 target present executions before abstentions/exact reuse. Publish
operational32-slot overhead separately; no method gets uncharged target previews.
Fixed3 must be generated with its unchanged native hold schedule, not inferred
from a member of the new spatial control grid. Preserve the exact selected qpos
reference and native request; no present result may change selection.

Each target scene must spawn its assigned beam center, underside, length, width
and thickness. The old A5 apparatus validator hardcodes a0.24m beam length: a
new geometry-aware readback validator must test both commanded and actual USD
extents, poses, neighbor isolation and actual collision-enabled status. Do not
reuse that validator unchanged for A19's0.32/0.48m beams. Keep the proven32-slot
context strategy and frozen tracker/checkpoints/default encoder. Preregister
present context, no-edit controls and all job overhead before launch.

Primary endpoint: all-assigned obstacle-present local passage with the unchanged
complete1s continuation, geometry/corridor/upright/deadline requirements. Report
nominal geometric penetration,40mm margin violation and physical contact evidence
separately. A18 did not validate force sensitivity; absent force records cannot
support a contact-free label. Even successful future foot/shin/torso controls
would not establish coverage of every overhead-beam contact surface.

Report paired gains/losses, scene-level distribution, absent-qualified selections
that fail present execution, and their cutoff/clearance/progress failure modes.
No selected-only experiment estimates how many rejected candidates could pass.
Abstentions remain non-completions. Complete-route traversal remains separately
reported; a successful local passage must not be described as a finished route.

A constructive present result on multiple scenes would permit development of the
small response model. It does not establish efficient adaptation or holdout
transfer. The later two-preview pilot must include best fixed joint command,
nominal-preserving shallow and matched-space search, with whole-scene training
splits and paired gains AND losses.
