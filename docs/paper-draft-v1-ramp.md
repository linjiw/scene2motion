# Scene2Motion: Response-Adaptive Motion Programs for Frozen Humanoid Priors

**Draft v1 skeleton — 2026-08-31 (RAMP program).** Successor to `paper-draft-v0.md`, which is
**frozen as the baseline technical report** per `guidance-2026-08-31-ramp-refocus.md`. This is
a working skeleton for the team — every section states (a) what goes in it, (b) which existing
results and figures migrate there with their repo artifacts, and (c) which new result must be
produced (E1–E4). Precision over polish; nothing here is reviewer-facing prose yet.

**Alt title (noted, second choice):** *Scene2Motion: Event-Aligned Adaptation of Frozen
Humanoid Motion Priors for 3D Traversal.* "A frozen motion prior is not a planner" survives
only as an Introduction punch line, never as a title.

**Ground rules inherited from the refocus (apply to every claim below):**
- Audit / τ(d) / Kimodo audit are baseline, regression test, and evaluation infrastructure —
  never the first contribution.
- Scope fixes from `guidance-2026-08-31-ramp-refocus.md` §"Claim fixes" are binding:
  "43-D collapses to one axis" → scoped to tested request families / sampler / embodiment /
  metrics; "teacher is the ceiling" → scoped to this fitted QP response model under this shift;
  "exact collision geometry" → "the robot's simulation collision model with a separately
  measured coverage margin"; "repairs every failure" → X/Y under fixed budget; throughput
  split into raw / kinematically scored / accepted / SONIC-executed tiers with measured
  wall-clock; defect catalogue → provenance paragraph + appendix; dataset claim must show
  downstream utility (RepairNet data engine); "certificate" → "execution-calibrated
  acceptance rule".

---

## Abstract (draft, from guidance 十 — adapted to what the repo actually has)

> Large humanoid motion priors provide diverse and natural whole-body behaviors, but their
> conditioning interfaces do not directly expose a scene-grounded action space: requested
> adaptations may be distorted, temporally misplaced, or lost during physical tracking. We
> introduce Scene2Motion, a response-adaptive planner that converts frozen motion priors into
> scene-conditioned traversal modules without updating their weights. Given a route through 3D
> clutter, Scene2Motion decomposes the scene into path-aligned interaction events and
> transports coherent motion-adaptation residuals — such as ducking, stepping over, and
> lateral squeezing — onto the gait phase of a nominal motion generated by the prior. It then
> measures whole-body clearance, contact, and event-timing errors and updates the structured
> motion program using a locally identified response model or an amortized repair policy. A
> tracker-conditioned outcome model predicts conservative executed clearance and enables
> execution-aware route selection. Unlike verifier-only test-time selection, Scene2Motion uses
> observed failures to modify subsequent generator commands. We evaluate the method with
> frozen ARDY-G1 and Kimodo-G1 priors, the SONIC whole-body tracker, and held-out procedural
> traversal environments. Scene2Motion improves executed traversal success by **[X]**,
> outperforms equal-budget independent resampling by **[Y]**, and transfers
> response-conditioned repair across priors with **[Z]** retained improvement. Our analysis
> further shows that semantic behavior elicitation alone does not imply spatially placed or
> physically executable traversal, motivating feedback in the realized motion-response space.

Adaptations vs the guidance text, on purpose:
- Dropped "receding-horizon replanning" — nothing in the repo implements or schedules it;
  reinstate only if it lands.
- "held-out procedural traversal environments" (not "and scene-aware"): MTC data is **not in
  the repo** (see Scoping notes). "MTC holdouts" enters the abstract only after the data does.
- [X] slot fills from E1+E2 primary endpoints; [Y] from E2 (RepairNet-3 vs Best-of-3); [Z]
  from E3 retained-gain ratio. Placeholder discipline: no number appears here that the
  evidence-status table below does not mark **landed**.
- "improves executed traversal success" is measured against the strongest frozen baselines we
  ourselves built (scalar secant repair, best-of-B) — the abstract must name the comparison
  when the numbers land, per claim-fix 4.

---

## Contributions (three, per guidance 九 — not five)

### C1. Prior-compatible scene motion programs

**Claim.** An event-aligned, phase-aware scene adaptation representation: per obstacle
e_j = (s_j path progress, Δs_j extent, g_j geometry, b_j body region, m_j mode, φ_j target
gait/contact phase, a_j continuous params), realized as **coherent residual packets**
ΔP = P_adapt ⊖ P_nominal transported onto the current seed's own phase-matched nominal walk
with warp W_{γ,τ,ρ} (amplitude, path shift, time scale). This puts duck, step-over, and
squeeze at the correct path position and gait phase while preserving the seed's gait style,
swing side, velocity, heading, and history — "the foot in swing when the robot reaches s_j
peaks its clearance arc at the obstacle, stance foot keeps contact", never "frame 104, left
foot +8 cm". Novelty is *not* "primitives" (MotionBricks owns that); it is scene-aligned
residual programs corrected by realized response.

**Evidence slot.** E1 (residual-packet pilot) is the deciding experiment. Design basis
migrates from v0 §6 (interface mechanism: absolute-height vs root-relative encoding,
rotations as the clean leg channel — `REPORT.md` §4.2/§4.4, exp008/exp010 ledgers) and from
the exp015b failure (text elicits the behavior but places it nowhere —
`outputs/exp015b_spatial_reanalysis/receipt.json`: box clearance 0.0 m in 8/8, peak-x sd
≈ 0.70 m). Donor machinery already exists: `scene2motion/semantic_scaffold.py` (donor bank,
phase-matched selection, obstacle-centered transplant) — currently absolute-pose transplant;
E1 upgrades it to residual form.

### C2. Response-adaptive repair

**Claim.** Repair driven by the *measured* response r_k = Φ(S, u_k, x_ref, x_exec) — signed
clearance trace, collision body/position, achieved dip, widths, swing peak, stance loss, root
deviation, execution loss, repair history — first through a **local response optimizer**
(paired-stream finite differences → local Jacobian → small QP over ~7 event parameters with a
packet-coherence constraint), then distilled into an amortized **RepairNet**
Δu_k = π_φ(S, u_k, r_k). Under a fixed generation budget this *modifies the program from
observed failure* rather than re-rolling or re-ranking — the thing Texedo/BRIC/ReactiveBFM/
SafeFlow do not do. Key ablation: scene-only vs program-conditioned vs response-conditioned
vs response-conditioned-without-coherent-projection; if the third beats the first two, the
method conclusion is "closed-loop learning should occur in response space, not only in
scene-to-command space."

**Evidence slot.** E2 (feedback vs equal-budget best-of-B) plus the ablation table. The
scalar duck secant repair is the L0 baseline it must beat
(`outputs/phase4e_architecture_v2_s8/experiment.json`: 8 paired seeds × 36 OOD scenes, 4 320
runs — TCN .729 → +2 repairs .993 vs best-of-3 .774; heuristic+1 1.000/.622/1.5). The TCN is
the open-loop baseline (same artifact). Zero-regression accounting and at-risk denominators
carry over as protocol.

### C3. Execution-aware cross-prior planning

**Claim.** SONIC outcomes train an executed-clearance model (τ(d)-style, tracker-conditioned)
that enters the *planning objective*, not just a gate: route cost
C(π) = L(π) + λĴ_adapt(π) + η p̂_exec-fail(π) + κ N̂_calls(π), so body adaptation cost and
predicted execution risk change which route is chosen. Cross-prior: response-conditioned
repair trained on ARDY outcomes transfers zero-shot to Kimodo-G1 — scoped strictly to "two
released priors sharing the G1 embodiment", never "arbitrary priors".

**Evidence slot.** E4 (route–body joint planning) and E3 (ARDY→Kimodo response transfer).
Data sources landed: SONIC achieved-state export (`scene2motion/sonic_state_export.py`;
`run/receipt.json` — exp1b_execution_clearance, 859 verified selections, 36 scenes, 526
unique clips, SONIC commit fb57e86 pinned) and the Kimodo reduced-audit harness
(v0 §4: 4.5× overcount, duck ladder stability 1.00, 84 clips / 173 s). Route-selection
baseline machinery: `outputs/phase4b_select` (Experiment E, `REPORT.md` §13).

---

## Section skeleton (1–8, per guidance 九)

### §1 Introduction

**(a) Contents.** Opportunity (frozen priors as motion substrate); the hard problem stated as
online system identification and inverse control of a frozen, stochastic, context-dependent
generative actuator: u\* = argmin_u E_{z,ξ} J(S, T(G_θ(u, z, h))); the core hypothesis
(event-aligned programs + realized-response feedback expand usable capability); the three
contributions. Punch line "a frozen motion prior is not a planner" appears once, here. NO
strawman framing: priors provide the substrate but no scene-grounded action space — do not
write "they claim to be planners" (ARDY/Kimodo claim controllable generation; CLoSD is the
diffusion-as-planner claimant).

**(b) Migrates here.**
- **Fig. 1 motivation case = text step-over failure** (十一: "Text step-over failure → Fig. 1
  motivation"): prompt raises the swing foot +56.9 mm (3.6 σ) yet obstacle-centered box
  clearance is **0.0 m in 8/8 seeds**, swing placement uncorrelated with the obstacle
  (peak-x sd ≈ 0.70 m, same as walking). Artifacts: `outputs/exp015/receipt.json`,
  `outputs/exp015b_spatial_reanalysis/{receipt.json,rows.jsonl}`; v0 §6 "Text selects
  behaviors; it does not place them".
- **6× counting result as a motivating figure, not a headline** (十一): the counting funnel
  10.00 → 1.67 → ~1, one panel. Artifacts: `outputs/audit_delta.json`,
  `experiments/audit_delta.py`, v0 Table 1.

**(c) New result needed.** None (framing only) — but Fig. 1 should gain a second panel from
E1: the same scene where a residual packet + response repair *succeeds*, once landed.

### §2 Related Work

**(a) Contents.** Five subsections per guidance: (i) controllable motion priors (ARDY
2607.08741, Kimodo, MotionBricks 2604.24833); (ii) scene-aware motion planning (CLoSD
2410.03441, MTC 2603.05993); (iii) test-time guidance and verifier selection (Texedo
2606.22998, BRIC 2511.20431, ReactiveBFM, SafeFlow) — position RAMP as modifying the program
from observed failure, not selecting/rejecting/adapting-the-tracker; (iv) physics alignment
and tracking (SONIC, GenTrack, post-training lines); (v) motion primitives and scene-aligned
data (MotionBricks, CLAW 2604.11251, MTC).

**(b) Migrates here.** v0 §2 text, re-cut into the five bins; the "orthogonal to perceptive
whole-body RL" paragraph survives.

**(c) New result needed.** None.

### §3 Problem Formulation

**(a) Contents.** The composite stochastic actuator S, u → G → x_ref → T → x_exec; formal
objective u\* = argmin_u E_{z,ξ} J(S, T(G_θ(u,z,h))); why u is a soft condition, not an
action (stochastic, phase- and history-dependent, coupled channels, nonlinear OOD response,
no inverse model, tracker distortion). Define the event program e_j = (s_j, Δs_j, g_j, b_j,
m_j, φ_j, a_j) and the program space here so §4 can use it.

**(b) Migrates here.** The measurement definitions that make J concrete: directional
clearance split (overhead vs lateral, `scene2motion/verify/trace.py`, `REPORT.md` §9);
collision model = robot's simulation collision primitives + measured 4 cm coverage margin
(`scene2motion/robot.py`, `BODY_MARGIN`); paired-seed / null-calibration machinery referenced
forward to §5.

**(c) New result needed.** None (definitions), but the obstacle-centered whole-body box
clearance probe (`scene2motion/stepover_eval.py`, `BoxHeightProbe`) is promoted to a named
primary endpoint here.

### §4 Scene2Motion-RAMP (the method)

**(a) Contents.** Six components, in pipeline order:
1. **Event representation** — path-progress events, never frame schedules (fixes seed-phase
   variation, speed, placement, prompt window timing, autoregressive drift).
2. **Residual packet construction** — ΔP = P_adapt ⊖ P_nominal (pelvis vertical, swing-arc,
   hip/knee/ankle rotations, root-speed, torso/arm counterbalance, timing residuals);
   transport P_target = P_nominal ⊕ W_{γ,τ,ρ}(ΔP) onto the current seed's phase-matched
   nominal walk.
3. **Response measurement** — r_k = Φ(S, u_k, x_ref, x_exec), the full deficit vector.
4. **Local response optimizer** — u_k ± ε_i paired-stream counterfactuals → local Jacobian
   r(u_k+Δu) ≈ r_k + A_kΔu → QP: min ‖W(r\*−r_k−A_kΔu)‖² + λ‖Δu‖² + ηJ_deform s.t.
   ‖Δu‖_∞ ≤ δ, u_k+Δu ∈ U_packet. Control variables (~7): residual strength, event center
   shift, duration scale, root lateral offset, torso-yaw scale, foot-height scale,
   lead/recovery distance. Triple role: strongest non-learned baseline, repair-trajectory
   generator, RepairNet supervision.
5. **RepairNet** — Δu_k = π_φ(S, u_k, r_k); u_{k+1} = Π_U(u_k + Δu_k); trained with the
   repairability-aware curriculum (L0 monotone duck → L1 phase-sensitive step-over → L2
   multi-region squeeze → L3 multi-event → L4 discrete+continuous → L5 cross-prior), sampling
   the repairable frontier (one-shot fails, optimizer fixes in 1–2), with
   D_repair = Σ w_i d_i over clearance/phase/contact/execution/mode deficits.
6. **Execution-aware route cost** — C(π) = L + λĴ_adapt + η p̂_exec-fail + κ N̂_calls, with
   the executed-clearance model supplying p̂_exec-fail.

**(b) Migrates here (十一 rows).**
- **§6 mechanism → residual packet design basis**: absolute-height signals reach the body
  decoder directly, ground-plane signals pass only as velocity; rotations are the clean leg
  channel (+271.8 mm at 6.6 mm sd, ~41 σ) — this is *why* packets carry rotations + pelvis
  height + timing rather than ground-plane joint positions. Artifacts: `REPORT.md` §4.2/§4.4,
  `outputs/exp008`, `outputs/exp010`, v0 §6 and Fig. 4 (SNR contrast, reused as a method
  figure here).
- **Scalar duck repair → L0 baseline / Level-0 controller**: forward-secant gain inversion,
  3τ·v anticipation, strict monotonicity, ≤2 iterations. Code: `scene2motion/verify/loop.py`
  and the repair operator (`REPORT.md` §10); numbers from
  `outputs/phase4e_architecture_v2_s8`.
- **SONIC achieved-state export → execution-model data source**:
  `scene2motion/sonic_state_export.py`; landed dataset `run/` (exp1b_execution_clearance,
  859 rows / 36 scenes / 526 clips, post-reset-teleport truncation fixed).
- Donor/transplant machinery: `scene2motion/semantic_scaffold.py` (exp016's scaffold builder)
  becomes the absolute-packet arm of E1 and the starting point for residual extraction.

**(c) New result needed.** The method components 2, 4, 5, 6 do not exist as code yet (only
their ingredients do). Implementation order per guidance 十二: residual packets → local
optimizer → RepairNet → execution model in the route cost.

### §5 Experimental Setup

**(a) Contents.** Task families = the curriculum levels L0–L5 with their scene generators;
priors: ARDY-G1 (frozen, DDIM η=0, per-sample noise streams v2) and Kimodo-G1 (frozen);
tracker: SONIC (commit + ckpt pinned as in `run/receipt.json`); splits: scene- and
topology-level holdouts, calibration split by scene/topology never by clip; baselines:
one-shot, independent best-of-B (byte-identical proposal), scalar secant repair (L0),
open-loop TCN proposer, Texedo-style verifier-selection at equal budget; metrics: executed
traversal success, minimum executed clearance, integrated deformation, path length, generator
calls, wall-clock latency, contact violations, refusal rate — never success alone; statistics:
see "Statistics protocol" below (inlined here in the paper).

**(b) Migrates here (十一 rows).**
- **Paired-seed / q99 / stability audit → experimental protocol + analysis**: paired deltas
  against neutral-body controls on matched seeds, null-calibrated thresholds, stability gate,
  dedup, physics stage. Artifacts: v0 §4, `scene2motion/morphology.py`,
  `scene2motion/funnel.py`, `experiments/funnel.py`, `outputs/exp005f*`.
- **Provenance frozen ledgers → reproducibility paragraph**: checkpoint SHA-256s, code
  commit, tracked-diff hash per row; cache keyed by schedule hash × seed × sampler version ×
  repair iteration (`scene2motion/demo/cache.py`, cache_version 2, noise stream v2).
- Scene grammar and OOD hard set: `scene2motion/scenes.py`, the 36-scene 3–6-beam family
  (`outputs/phase4d_hard`, `outputs/phase4e_architecture_v2_s8`).

**(c) New result needed.** Scene generators for L1–L4 families (floor obstacles at
parametric placements, narrow slots, composed beam+floor sequences, left/right-forcing
layouts). E1's six placement strata (two absolute-history levels × phase within the 52-frame
horizon) come from `experiments/exp016_semantic_geometric_stepover.py`
(DEFAULT_TARGET_FRAMES) and generalize into the L1 generator.

### §6 Main Results

**(a) Contents.** Five result blocks: multi-behavior performance (E1), feedback vs sampling
(E2), cross-prior (E3), execution (τ(d)-gated executed success), route–body planning (E4).

**(b) Migrates here.**
- Table 2 (`outputs/phase4e_architecture_v2_s8/experiment.json`) as the **L0 baseline block**
  and the equal-budget-control protocol template (zero-regression accounting, at-risk
  denominators, saturation labeled `accepted_margin`).
- exp1b executed-clearance data (`run/`) feeding the execution block; v0 §7's two-gate
  acceptance rule (c_ref ≥ τ(d); c_ref ≥ 0.18 + τ(d)) with the ≥95 % executed-success-among-
  accepted target, reported as risk–coverage.
- Route-selection baselines from `outputs/phase4b_select` (`REPORT.md` §13: length-only
  agreement 0.139, tcn_body regret 2.70, TCN 649× batched-scoring speedup at k=128) as the
  E4 comparison scaffold.

**(c) New results — the four deciding experiments, with kill conditions and endpoints.**

**E1 — residual-packet pilot** (decides: representation bottleneck vs absent capability).
Tasks: overhead duck, floor step-over, lateral squeeze / turn-and-tuck. Arms per task:
current synthetic constraints · absolute donor packet · phase-matched residual packet ·
residual + 1 response repair · residual + 2 response repairs.
**Step-over primary endpoint (fixed, non-negotiable): obstacle-centered whole-body box
clearance + crossing position error + correct swing foot + stance-foot contact + SONIC
execution success — never foot peak.** (Probe exists: `scene2motion/stepover_eval.py`;
local-structure gates in exp016.) Baselines to beat are already measured: synthetic/text arms
sit at box clearance 0.0 m in 8/8 (`outputs/exp015b_spatial_reanalysis`), position-channel
lift killed at 24 seeds (`outputs/exp1c_stepover`: success 0.250 → 0.000 over 0.05–0.28 m,
bilateral flight 0.91–0.98 despite gating).
*Kill condition:* if residual packets (with ≤2 response repairs) do not beat the absolute
packet AND do not move the step-over endpoint off the floor (local-step success > 0 with
stance contact at matched SONIC success), the public conditioning interface is the
bottleneck → pivot to the output-space residual adapter x_adapt = x_nominal ⊕ A_ψ(S,
x_nominal) or per-window latent optimization — per guidance, keep designing the stronger
method, do not retreat to the audit paper.

**E2 — feedback vs equal-budget sampling.** Fixed generator budget B ∈ {1,2,3}; arms:
one-shot, independent best-of-B, current monotone repair, local response optimizer,
RepairNet. Core comparison: **RepairNet-3 vs Best-of-3** (Texedo makes verifier selection a
mandatory baseline). Metrics: the full §5 list, not success alone. Protocol template:
`outputs/phase4e_architecture_v2_s8` (its resample arms are the byte-identical-proposal
control). *Kill condition:* RepairNet-3 not beating Best-of-3 on executed traversal success
(scene-level cluster bootstrap, §Statistics) kills C2's "feedback beats sampling" claim; the
optimizer-only arm then decides whether the failure is amortization or the response model.
Ablation table (scene-only / program-conditioned / response-conditioned / no-coherent-
projection) lands here.

**E3 — ARDY → Kimodo zero-shot response transfer.** RepairNet trained on ARDY transitions
corrects Kimodo from Kimodo's *observed* response. Arms: scene-only proposer transfer ·
open-loop forward-model transfer · response-conditioned RepairNet transfer. Claim scoped to
two released G1-embodiment priors. Harness exists (Kimodo reduced audit, v0 §4: 4.5×
overcount, duck ladder monotone at stability 1.00 — Kimodo has the L0 axis to repair; ledger
path to be pinned under `outputs/` when migrated from the kimodo run tree). *Kill
condition:* if response-conditioned transfer retains no gain over scene-only transfer, drop
the portability claim and scope C3 to execution-aware planning only.

**E4 — route–body joint planning.** C(π) = L + λĴ_adapt + η p̂_exec-fail + κ N̂_calls.
Arms: shortest path · current mode-cost A* · RAMP-aware route cost · oracle over generated-
and-verified routes. Scenes must contain real trade-offs (short-and-deep-duck vs
long-and-upright; left-squeeze vs right-step-over; multi-obstacle composition). *Kill
condition (naming, per guidance):* if body adaptation never changes route choice vs mode-cost
A\*, the system is called a **local whole-body adaptation planner** and "planner" claims are
narrowed accordingly.

### §7 Analysis

**(a) Contents.** The audit material, demoted to analysis: naive capability counting; the
interface mechanism; text does not place behavior; failure modes of the repaired system.

**(b) Migrates here (十一 rows).**
- Counting funnel + Kimodo replication (4.5× vs 6×) as *why response feedback is necessary*:
  v0 §4, `outputs/audit_delta.json`.
- Interface mechanism (v0 §6, Fig. 4 SNR chart): duck dose-response tracked
  1.000/0.750/0.625 (`outputs/exp011`), position ≤1.6 σ sign-flipping, rotation ~41 σ.
- Text-places-nothing analysis: `outputs/exp015`, `outputs/exp015b_spatial_reanalysis`;
  exp016's factorial (text × scaffold) slots here once landed — it is the bridge between
  analysis and E1.
- Failure-mode taxonomy of repairs from the E1/E2 ledgers (new), replacing v0's defect-
  centric §8 narrative.

**(c) New result needed.** exp016 factorial readout (in flight — script committed at
`experiments/exp016_semantic_geometric_stepover.py`, no `outputs/exp016*` dir yet); repair
failure taxonomy from E2.

### §8 Limitations and Conclusion

**(a) Contents.** Two priors, one robot, one tracker; procedural scenes (MTC pending);
curriculum levels actually reached; the Scoping notes below stated plainly; conclusion =
the bar from the refocus: coherent residual + response adaptation turned placed step-over,
lateral squeeze, and composed traversal from failures into stable executable successes — or
it did not, and to what level.

**(b) Migrates to appendix / project page (十一 rows).**
- **Defect catalogue → appendix** as a table (phenomenon → false positive/negative it would
  have produced → control that caught it), generated from the ledgers; concise provenance
  paragraph in the main text. Artifacts: `REPORT.md` §5/§17, v0 §8.
- Provenance-frozen ledgers → reproducibility paragraph (~34 k generations).
- Refusal → a system output (quantified deficit, `scene2motion/planner.py`
  `_diagnose_refusal`; demo Fig. 3d), not an independent contribution.
- **§9 dataset generator → RepairNet data engine**: the pipeline emits (scene, program,
  response, repair, outcome) transitions; the downstream-utility experiment is RepairNet
  success ↑ / generator calls ↓ vs data scale. Seed corpus landed:
  `outputs/corpus_pilot_v2/receipt.json` (300 scenes / 301.9 s: 192 accepted at 0.18 m, 76
  collision-free below margin, 6 rejected, 26 refused; noise stream v2, cache v2). Throughput
  reported in the four tiers with measured wall-clock and GPU config — never a single
  "verified traversals/GPU-day" number.

---

## Evidence status table

Every claim the paper will make, with status and artifact. "Landed" = number exists under the
v2 sampler with a committed ledger; "in-flight" = harness exists / run started; "not
started" = neither.

| # | Claim | Status | Artifact / planned experiment |
|---|---|---|---|
| 1 | Naive counting overstates repertoire 6× (ARDY), ~10× after tracking | landed | `outputs/audit_delta.json`, v0 Table 1 (⟨v2⟩ recheck in standing revalidation) |
| 2 | Overcount + channel asymmetry replicate on Kimodo-G1 (4.5×) | landed (ledger path to pin under `outputs/`) | v0 §4 reduced audit; 84 clips / 173 s |
| 3 | Duck is addressable and executable with graceful dose-response | landed | `outputs/exp011`; `REPORT.md` §4.7 |
| 4 | Position-channel step-over fails at every amplitude (request family scoped) | landed | `outputs/exp1c_stepover` (24 seeds, pre-registered ladder) |
| 5 | Text elicits step-over but does not place it (box clearance 0.0, 8/8) | landed (v1 sampler, labeled post-hoc) | `outputs/exp015`, `outputs/exp015b_spatial_reanalysis` |
| 6 | Text × coherent scaffold factorial (semantic × geometric roles) | in-flight | `experiments/exp016_semantic_geometric_stepover.py`; no `outputs/exp016*` yet |
| 7 | L0 repair lifts every proposer to ≥0.99 collision-free, zero regressions, beats equal-budget resampling | landed | `outputs/phase4e_architecture_v2_s8/experiment.json` (4 320 runs) |
| 8 | Open-loop TCN inherits its teacher's OOD optimism (scoped per claim-fix 2) | landed | same artifact + `outputs/phase4d_hard`, `REPORT.md` §15 |
| 9 | Executed clearance measurable; achieved-state export validated | landed | `run/` (exp1b, 859 rows); `scene2motion/sonic_state_export.py` |
| 10 | τ(d) fit + risk–coverage of the execution-calibrated acceptance rule (≥95 % target) | in-flight | fit over `run/rows.jsonl`; v0 §7 spec; `docs/exec-gate-audit.md` schema |
| 11 | Residual packets beat synthetic + absolute packets on duck/step-over/squeeze (E1 endpoint) | not started | E1; donor machinery in `scene2motion/semantic_scaffold.py` |
| 12 | Local response optimizer beats scalar secant on multi-axis tasks | not started | E2 arm; teacher for #13 |
| 13 | RepairNet-B beats Best-of-B at equal budget (core E2) | not started | E2; ablation table (scene/program/response conditioning) |
| 14 | Response-conditioned repair transfers ARDY→Kimodo zero-shot with retained gain | not started | E3 |
| 15 | RAMP-aware route cost changes route choice and wins vs mode-cost A* | not started | E4; baseline scaffold `outputs/phase4b_select` |
| 16 | Data engine: RepairNet success ↑ / calls ↓ with transition-data scale | not started (seed corpus landed) | `outputs/corpus_pilot_v2` + E2 training curves |
| 17 | Route selection that prices the body beats length-only (baseline block) | landed | `outputs/phase4b_select`, `REPORT.md` §13 |
| 18 | Repairability-aware curriculum improves RepairNet over difficulty-ordered sampling | not started | E2/E3 training ablation (L0–L5 pools, D_repair) |

Rule: abstract placeholders [X]/[Y]/[Z] map to rows 11+13 ([X]), 13 ([Y]), 14 ([Z]) and stay
bracketed until those rows read **landed**.

---

## Statistics protocol (inlined into §5; binding for every table)

- **Scene is the inference unit.** Seeds are nested within scenes and estimate within-scene
  stochasticity; they are never pooled as independent samples. (Table 2's 288 rows/cell = 36
  scenes × 8 seeds → n = 36.)
- **Primary analysis:** scene-level mean success + cluster bootstrap over scenes; or
  mixed-effects logistic regression with scene as random effect. Pre-register which, per
  experiment, before the run.
- **McNemar is auxiliary only** (paired, seed-pooled) and never carries a headline alone.
- **Conformal / calibration splits are by scene and topology, never by clip.** The τ(d)
  calibration set and its test set share no scene.
- **Terminology:** "execution-calibrated acceptance rule", not "certificate"; report held-out
  risk–coverage, not a single rate.
- Zero-regression claims always state the at-risk denominator (protocol from
  `outputs/phase4e_architecture_v2_s8`); saturated repairs labeled `accepted_margin` with the
  binding limit named; every 1.000 carries its Wilson 95 % lower bound.

---

## Honest scoping notes (reviewer-flagged; keep in the draft, not just in our heads)

1. **Lateral squeeze may be robot-capped, not prior-capped.** Tucking buys ~32 mm before the
   thighs bind (optimized over all 14 arm DOFs, minimum half-width 0.2065 m) against a 55 mm
   seed-noise σ (`REPORT.md` §4.5). If E1's squeeze arm confirms this, **L2 becomes
   route+timing adaptation** (torso yaw + root lateral offset + speed through the slot)
   rather than body-narrowing — write the curriculum level that way from the start rather
   than walking it back later.
2. **exp016's SONIC stage runs without the obstacle in Isaac** (the obstacle exists only in
   the Scene2Motion collision world; `experiments/exp016_semantic_geometric_stepover.py:687`).
   Executed readouts there measure **clearance retention under tracking, not contact
   physics** — a tracked foot that would strike the box is detected by replaying achieved
   qpos against our geometry, not by Isaac contact. E1's "SONIC execution success" endpoint
   inherits this until obstacles are placed in the Isaac stage; say so wherever the endpoint
   is reported.
3. **MTC data is not in the repo.** The natural residual source (348 traj / 145 scenes,
   lateral clearance / height adaptation / asymmetric adjustment) is cited but unavailable
   here; **donors are currently self-elicited** — ARDY's own step-over-prompt generations
   filtered by exact qpos foot envelopes (`scene2motion/semantic_scaffold.py` donor bank).
   Consequence: E1's residual packets are same-prior residuals; MTC-derived residuals are an
   upgrade path, not a dependency, and no claim may assume them until the data lands.
4. **exp015/exp015b numbers are legacy noise-stream v1** (labeled `post_hoc_exploratory` in
   the receipt). Fine for Fig. 1 motivation with the label; any headline use requires the v2
   rerun per `docs/revalidation-2026-08-30.md`.
5. **Kimodo reduced-audit caveats carry over** (vendored joint-point descriptors, scene-free
   validity rule, one route): paired deltas comparable, absolute extents not — E3 must
   re-measure Kimodo through `G1Body` geometry before its numbers meet ARDY's in one table.

---

## Figures & tables to produce (delta vs v0's list)

- **Fig. 1** Motivation pair: text step-over clearing 0.0 m with random placement (exp015b)
  → same scene under residual packet + response repair (E1, once landed).
- **Fig. 2** System: scene → route events → prior-compatible adaptation program → frozen
  prior → measured response → program repair → execution-aware selection (the guidance's
  pipeline formula, drawn).
- **Fig. 3** Residual packet construction: donor ⊖ nominal → W_{γ,τ,ρ} → transport onto
  current seed's phase (uses exp016 scaffold assets).
- **Table: E1 matrix** (3 tasks × 5 arms, primary endpoints per task).
- **Table: E2 budget matrix** + conditioning ablation; Table 2 reprinted as the L0 block.
- **Fig. 4** (migrated) channel SNR contrast — now captioned as residual-packet design basis.
- **Fig. 5** τ(d) risk–coverage with the ≥95 % acceptance point (from `run/` fit).
- **Fig. 6** E4 route flips: scenes where p̂_exec-fail changes the chosen route.
- **Fig. 7** Data-engine curves: RepairNet success / generator calls vs transition count.
- **Appendix** defect-catalogue table, ledger manifest, curriculum pool definitions
  (D_repair weights), full statistics receipts.

## Execution order (mirrors guidance 十二; no GPU runs scheduled by this document)

1. Residual-packet pilot (E1) — highest information: representation bottleneck vs absent
   capability. Fallback on kill: output-space residual adapter / per-window latent
   optimization, not retreat to the audit paper.
2. Local response optimizer (E2 non-learned arms).
3. RepairNet + repairability-aware curriculum (E2 learned arm, #18).
4. SONIC execution model inside program + route optimization (τ(d) fit → E4).
5. ARDY→Kimodo transfer (E3).
