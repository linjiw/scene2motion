# Paper-readiness audit — 2026-09-03

Produced by a 141-agent audit workflow over the manuscript, the figures, the vocabulary rules and
the committed evidence. Eight independent auditors proposed findings; each medium/high finding was
then attacked by two independent verifiers through different lenses (correctness and
source-tracing) and kept only if it survived both. **17 findings survived; 49 were refuted** — the
refutation rate is the point: most apparent discrepancies were the auditor misreading a receipt,
and those never reached this list.

Every item below names the file to change and the receipt key that settles it. Line numbers are at
commit `620bafa`.

---

# Scene2Motion paper v2.2 — action list

All paths absolute. Paper = `/home/linjiw/scene2motion/docs/paper-draft-v2-2026-09-02.md` (line numbers at HEAD `620bafa`).

## 1. Must fix before submission

1. **L246–247** — replace "All arms share a maximum of three generations" with "budgets differ by arm: one generation uncorrected, two for one correction / best-of-two, three for two corrections / best-of-three; only the two-correction and best-of-three arms Table III compares are equal-budget" — `/home/linjiw/scene2motion/outputs/phase4e_architecture_v2_s8/experiment.json` → `method_specs[*].max_adapted_generations` = 1/2/3/2/3 per proposer.
2. **L267 (and its duplicate in §VI-A)** — "raises the 5 cm count to 20 of 64" → "24 of 64 (union over 0.05 m grid centres in [0.95, 1.45] m; 17 of 64 at ±0.10 m)", kept labelled as capability not placement — recomputed from `/home/linjiw/scene2motion/outputs/analysis_exact_centre_cost_curve/exact_hits.npz` (`hits`, `centres_m`, `heights_m`); the 20 exists only in `/home/linjiw/scene2motion/docs/ramp-exp021-addressable-region-2026-09-01.md:91` (0.312 × 64), which `CLAUDE.md`'s non-reproducing table already voids.
3. **L410–413** — drop "a 14 s clip cap prevented most rollouts from reaching the beam; that is a limitation of the protocol"; write "0 of 859 rollouts preserved clearance; 554 ended at the evaluator's tracking-error cutoff and only 200 reached the first beam, so the endpoint was rarely reachable; the ~14 s clip length bounds the 305 rollouts that were not cut off" — `/home/linjiw/scene2motion/outputs/exp1b_execution_clearance_v2/receipt.json` → `outcomes.terminated` 554, `outcomes.reached_first_obstacle` 200, `outcomes.passed_last_obstacle` 0.
4. **L242 + §VI-A L275** — say the positive control failed: "the sideways-step positive control was refused at its preregistered substrate gate (0 of 8 sidestep composites against a required 4), so the delayed-switch arm has no working positive control" — `/home/linjiw/scene2motion/outputs/exp023b_prompt_switch_control/receipt.json` → `status: "refused"`, `refusal_reason: "squeeze0_substrate_gate_failed"`, `measurement_gates.squeeze0_substrate.observed_present` 0 / `required_min_present` 4; the delayed arm is retained by design (`measurement_gates.delayed_arm_absence_is_an_outcome_not_a_gate: true`).
5. **L345** — "the 20 references with no positive clearance" → "the 20 references that do not clear a 3 cm box anywhere (15 of them have no positive clearance at all)" — stratum defined at `/home/linjiw/scene2motion/experiments/analyze_trackability_contract.py:435` (`lift_height_m < 0.03`), `/home/linjiw/scene2motion/outputs/analysis_trackability_contract/receipt.json` → `summary.exp021_non_lifting_stratum.n` 20; `/home/linjiw/scene2motion/outputs/analysis_event_frames/receipt.json` → `summary.exp021.non_lifting_clips` 15.
6. **L73–74** — "44 of 64 … clear a 3 cm box somewhere while only 12 clear a 5 cm box where the scene puts it" → like-for-like "37 of 64 clear a 5 cm box somewhere while only 12 clear it where the scene puts it" — `/home/linjiw/scene2motion/outputs/exp021_elicited_lift_distribution_v2/receipt.json` → `summary.n_clearing["0.05"]` 37; `/home/linjiw/scene2motion/outputs/analysis_pool_coverage/summary.json` → `results[0].by_height["0.05"].n_reference_clears` 12.
7. **L274** — "a step 3.2 m from its anchor" → "a median 2.30 m from its anchor (mean 2.06 m; residual arm median 1.32 m); 3.2 m is the worst of eight seeds" — `/home/linjiw/scene2motion/outputs/exp019_gait_matched_stepover_v7/placement_analysis.json` absolute-arm `|signed_placement_error_m|` = [0.46, 0.50, 1.38, 1.60, 3.00, 3.16, 3.17, 3.24]; `/home/linjiw/scene2motion/outputs/exp020_text_only_control/receipt.json` → `summary.absolute.mean_abs_placement_error_m` 2.064.
8. **L192–193** — "calibrated on a corpus of tracker-successful references before any stepping experiment" → "calibrated on 284 clips from the EXP-1C control arms — 144 ARDY references at 25 fps plus 140 achieved SONIC rollouts at 50 Hz — frozen before EXP-021" — `/home/linjiw/scene2motion/outputs/exp016_threshold_calibration/receipt.json` → `calibration.n_clips` 284, `corpus.arms` = ctrl-l05…ctrl-l28, `corpus.sources` "both"; same wording must change at `/home/linjiw/scene2motion/docs/project-goal-2026-09-02.md:96`.
9. **L329** — "Nothing falls, stalls or times out" → "no rollout fell; the timeout class was disabled for this analysis so its zero is 'not assessed'; 'stalled' is the residual class and is preempted by the cutoff and collision classes, so read it with the 50 of 64 that never reached the obstacle" — `/home/linjiw/scene2motion/experiments/analyze_traversal_outcomes.py:83` constructs `TraversalCriteria` without `time_limit_s`; `/home/linjiw/scene2motion/scene2motion/traversal_eval.py:74–75` ("`None` disables the timeout class") and `:165` (precedence). Fall thresholds *do* have defaults (`:76–77`), so "nothing falls" stands.
10. **L344** — "Maximum foot lift alone gives AUC 0.92" → "the maximum clearable whole-body box height anywhere on the route gives AUC 0.92" — `/home/linjiw/scene2motion/outputs/analysis_trackability_contract/receipt.json` → `summary.exp021_lift_height_only.auc` 0.9237, whose field comes from `box_height_profile()` in `/home/linjiw/scene2motion/experiments/analyze_e1a_placement.py:57-76`; the real foot feature `max_foot_clearance_m` is 0.878 in the same receipt.
11. **L285 vs L316** — renumber: §VI-B coverage table = Table I, §VI-C outcome matrix = Table II, repair tables → III and IV; both lines currently say "Table I" for different tables.
12. **Table II (L367–379)** — add the rejected-before-execution column required by the paper's own rule at L200–202: uncorrected TCN 27.1 %, TCN best-of-three 22.6 %, QP uncorrected 12.9 %, heuristic uncorrected 1.7 % — `/home/linjiw/scene2motion/outputs/phase4e_architecture_v2_s8/experiment.json` → `summary.*.rejected_rate` (they are exactly the complement of `collision_free_rate`).
13. **L346–348** — harden the sweep wording to "runs of eight frames or longer ($r_{\max} \ge 0.32$ s, i.e. > 0.28 s): 51 of 53 rejected, 11 of 11 passed" so nobody rewrites it as "> 0.32 s" — `/home/linjiw/scene2motion/docs/figures/fig5_numbers.json` → `panel_b.operating_points["post hoc ≥ 0.32 s"]` (51/53, 0 false flags).
14. **L310–312** — "All 53 stopped rollouts were upright" is supported but add "one of the 53 has a torso up-axis below 0.92" — `/home/linjiw/scene2motion/outputs/analysis_trackability_contract/receipt.json` → `summary.fall_vs_cutoff_at_last_sample.n_up_z_below_0p92` 1.
15. **L466–469** — replace the placeholder reference block with the twelve full entries at `/home/linjiw/scene2motion/docs/codex-20260902-112948-EDT.md:331-353`.
16. **`/home/linjiw/scene2motion/docs/figures/fig5_numbers.json`** — regenerate via `/home/linjiw/scene2motion/experiments/fig_contract_gate.py` so `inputs.receipt_sha256` becomes `6c0c73ce0d29578064714e8d42451ee2c7f7c32cfd2ea8a7fa4519186443612b` (verified by `sha256sum` on `/home/linjiw/scene2motion/outputs/analysis_trackability_contract/receipt.json`); the recorded `be3e2938…` resolves only to the dirty-tree version at commit `0379d47`, and `fig6_numbers.json` already binds the current one.

## 2. Evidence on the shelf

- **The screen transfers to a second behaviour family.** `/home/linjiw/scene2motion/outputs/analysis_duck_contract/receipt.json` → `summary.decision.pooled` contact AUC 0.674 vs speed 0.441, `contact_minus_speed` +0.233 (CI 0.120–0.362), verdict `contract_transfers_to_the_duck_family`, 526 clips / 36 scenes, preregistered direction. **Belongs in §VI-C** — it is the answer to "the screen is a stepping artefact", and it is entirely absent from the paper.
- **Cross-family and cross-corpus AUCs.** Same receipt: `summary.exp1c_all` 288 clips AUC 0.974, `summary.exp1c_lift` 0.921, `summary.transfer_auc` 0.916 / 0.971 / 0.904, and `summary.exp1c_ctrl_calibration_corpus` 0.513 (the calibration corpus, no predictive claim). **§VI-C**, one sentence.
- **The screen rejects the whole producing set.** `summary.exp021_lift_ge_3cm` = {n 44, terminated 43, `n_within_calibrated_gate` 0} and `summary.exp021_within_calibrated_gate` = {n 8, survived 8}. **§VI-C** — states the conflict TEXEDO framing asks for: everything that clears fails the screen, everything that passes the screen survives.
- **The post hoc position choice, quantified.** `/home/linjiw/scene2motion/outputs/analysis_pool_coverage/summary.json` → `results[1]` (unstaged, x = 3.6 m): 2 of 64 clear at every height, N90 73 draws. **§VI-A or §VII** — makes the "chosen after inspecting the clips" caveat a number instead of an apology.
- **Everything that got past the box went through it.** `/home/linjiw/scene2motion/outputs/analysis_traversal_outcomes/summary.json` → `by_height["0.05"].per_clip`: 14 of 14 `passed_obstacle` rows are `collided_obstacle`, and exactly 1 rollout reached the goal region. **§VI-C** — resolves the apparent tension between "14 pass and finish beyond" (Table I) and "0 satisfy the endpoint".
- **The position channel has an executed outcome, not just a kinematic one.** `/home/linjiw/scene2motion/outputs/exp1c_stepover/receipt.json` → `per_amplitude.*.sonic_lift.success` 0.250/0.083/0.042/0.000/0.000/0.000 against `sonic_ctrl.success` 0.917–1.000 at 24 seeds each, overshoot 1.68–2.32×, verdict "claim KILLED on this ladder". **§VI-A** currently reports only the support-test failure and drops the tracked comparison.
- **The EXP-024 arms trade placement against the screen.** `/home/linjiw/scene2motion/outputs/exp024_reference_contract/receipt.json` → `stages.score.reference_summary_per_arm`: pinning heading *lengthens* the median run to 0.74 s, and pinning root height cuts clears-at-position from 7/32 to 1/32 (pin_yh 2/32). **§VI-C prospective paragraph** reports only the two favourable medians.
- **First physical-obstacle evidence in the project.** `/home/linjiw/scene2motion/outputs/probe_obstacle_present/report_h0.3.json` → `comparison.per_motion`: s4434 max root x 6.06 m without the box, 0.29 m with it, cutoff false→true. Marked `campaign_evidence: false`. **§VIII** — cite as an operational probe to show the promised experiment is now buildable, never as a result.
- **QP collision-free paired row.** `/home/linjiw/scene2motion/outputs/analysis_repair_paired_bootstrap/summary.json` → `results[2]`: +2.8 pp (CI 0.0–6.9), 8:0 discordant. **Table III** for completeness of the six-row analysis.
- **The tracked ducking negative never touched the learned proposer.** `/home/linjiw/scene2motion/outputs/exp1b_execution_clearance_v2/receipt.json` → `selection.methods` = heuristic, heuristic+1, heuristic+2 only. **§VI-E / §VII** — the arm carrying the headline repair gain (TCN) has never been tracked at all.

## 3. Figures to make

- **FIG-1-PIPELINE (L212) — does not exist.** No asset under `/home/linjiw/scene2motion/docs/figures/`. Needs drawing: scene → prompt/controls → reference → route-coordinate measurement → three paths (correct / reject-with-reason / track) → achieved-state scoring.
- **FIG-2-FUNNEL — already built, just bind it.** `/home/linjiw/scene2motion/docs/figures/fig2_channel_funnel.pdf` + `_numbers.json` (six channels: text STEP 64, exp1c position lifts 144, two packet arms, delayed prompt 16, free walk 16) shows counts at each evidence level per way of asking — exactly what L268–269 promises.
- **FIG-3-SCREEN — already built.** `/home/linjiw/scene2motion/docs/figures/fig5_contract_gate.pdf` (panel a run distributions, b ROC + both operating points, c upright-at-last-state, d cutoff-vs-onset). Regenerate first per item 16 above.
- **Screen across families — built and unused.** `/home/linjiw/scene2motion/docs/figures/fig6_screen_across_families.pdf` (3 corpora, 2 behaviour families) is the natural companion to the EXP-026 result in §VI-C.
- **Cost curve — built and unused.** `/home/linjiw/scene2motion/docs/figures/fig4_cost_curve.pdf` / `fig4_numbers.json` (`best_of_n_0.05.n90` 12, `0.08` 13, staged vs unstaged paired retention at six heights) is the figure for the N90 sentence at L278–279 and for the staged/unstaged contrast.
- **Channel response — built and unused.** `/home/linjiw/scene2motion/docs/figures/fig3_channel_response.pdf` carries the lift-height distributions and the packet compliance behind L271–276.
- **Repair vs resampling (new).** Paired rates + four discordance tiles (63:0, 104:0, 112:1, 18:41) from `/home/linjiw/scene2motion/outputs/analysis_repair_paired_bootstrap/summary.json`; spec already written at `/home/linjiw/scene2motion/docs/codex-20260902-112948-EDT.md:266`. Shows the win and the loss in one panel.
- **Outcome classes by box height (new).** Stacked bars over the six heights from `/home/linjiw/scene2motion/outputs/analysis_traversal_outcomes/summary.json` `by_height.*.outcomes`, with the timeout class drawn as "not assessed" rather than zero.

## 4. Still to run

- **EXP-024 tracked stage (128 references, flagged and passed).** Driver `/home/linjiw/scene2motion/experiments/exp024_reference_contract.py`; receipt is `status: "running"`, `stage: "predicted"`, `launches: {}`. Fills `[EXP-024-2X2]` / `[EXP-024-AUC]` at L356 and converts contribution (iii) from retrospective to prospective — the single largest change to the paper's standing.
- **EXP-028 termination-free rollouts + physics-seed re-roll.** Protocol `/home/linjiw/scene2motion/docs/ramp-exp028-termination-free-rollouts-protocol.md`, driver `/home/linjiw/scene2motion/experiments/exp028_termination_free_rollouts.py`. Fills `[EXP-028-OUTCOME]` (L357) and is the only thing that can turn "upright at the last archived state" into a physical outcome class; also supplies the step family's own test–retest ceiling (currently borrowed from exp1b).
- **EXP-024b known-trackable walk/jump controls.** Blocked on a format conversion: the asset `gear_sonic_deploy/reference/example/tired_one_leg_jumping_R_001__A359` is deployment CSV, not the motion pkl the bridge consumes (`/home/linjiw/scene2motion/docs/REPORT.md:1738-1745`). Without it §VI-C cannot rebut "the screen just penalises dynamic motion".
- **EXP-030 obstacle-present stepping.** Preregistered `/home/linjiw/scene2motion/docs/ramp-exp030-obstacle-present-stepping-protocol.md`, driver `/home/linjiw/scene2motion/experiments/exp030_obstacle_present.py` committed at `620bafa`, not launched. Its Q1 decides whether every replay-based execution sentence in the paper keeps its meaning or inherits a caveat; its Q2 is the first local-traversal-completion measurement.
- **EXP-029 ducking obstacle-present study.** Still `Status: draft, not preregistered` (`/home/linjiw/scene2motion/docs/ramp-exp029-selection-vs-coverage-protocol.md:3`). This is the experiment that decides evaluation contribution vs method contribution; §VIII promises it, so preregister before submission even if it cannot run.
- **Re-run `analyze_traversal_outcomes.py` under evaluator v2 with an explicit `time_limit_s`.** The working tree has an uncommitted rewrite of `/home/linjiw/scene2motion/scene2motion/traversal_eval.py` adding `EVALUATOR_VERSION = 2`, `timeout_assessed`, `n_timeout_assessed` and a completion-time deadline; its own comment says v1 "timed a deadline against the archive's length rather than against finishing" and mis-rated collide-then-fall runs. The committed `/home/linjiw/scene2motion/outputs/analysis_traversal_outcomes/summary.json` is v1 output, and the paper's outcome table is v1. Commit the module, re-run, refresh the table.
- **Commit the ±r union as a receipt key.** `/home/linjiw/scene2motion/experiments/analyze_exact_centre_cost_curve.py` writes `exact_hits.npz` but no scalar; add `tolerant_union` at ±0.10 / ±0.25 m per height so item 2 above cites a JSON key rather than a derivation.
- **Mechanism baseline (fixed extra-crouch under the same budget).** Named as untested at L379–381 and as "not run" in the claims table of `/home/linjiw/scene2motion/docs/project-goal-2026-09-02.md`. Cheap, CPU-side of the loop, and it decides whether the repair claim is "measurement helps" or "a bigger crouch helps".
- **Resolve the packet-placement discrepancy before citing either number.** `/home/linjiw/scene2motion/docs/REPORT.md:1412` reports medians 2.33 m / 1.62 m; `/home/linjiw/scene2motion/outputs/exp019_gait_matched_stepover_v7/placement_analysis.json` gives 2.302 m / 1.324 m. One of the two is wrong and the paper must not inherit it.
- **EXP-025 Kimodo cross-prior.** Protocol preregistered (`a665a01`), driver `/home/linjiw/scene2motion/experiments/exp025_kimodo_cross_prior.py` still untracked in the working tree. Only route to "not one prior's quirk"; optional for this submission, decisive for the limitation section.

## 5. What is already solid — do not re-litigate

- **Coverage decomposition (Table I).** 12 / 11 / 0 / 14 / 50 / 0 at the 5 cm box reproduces exactly from `/home/linjiw/scene2motion/outputs/analysis_pool_coverage/summary.json` `results[0]`, with N90 12 (5 cm) and 13 (8 cm) from `coverage_curve_reference.n90_budget_fresh_draws`. The disjointness claim is arithmetic, not inference.
- **Repair tables II and III.** Every rate in Table II matches `/home/linjiw/scene2motion/outputs/phase4e_architecture_v2_s8/experiment.json` `summary.*` (TCN 0.7292→0.9931 / 0.0035→0.375, resample3 0.7743 / 0.0139, heuristic 0.9826→1.0 / 0.5347→0.6458 vs 0.7257; generations 2.72 / 2.99 / 1.84 / 1.81), and all four Table III rows plus intervals and discordant pairs match `/home/linjiw/scene2motion/outputs/analysis_repair_paired_bootstrap/summary.json`. The abstract's 72.9→99.3 vs 77.4 comparison is correctly budget-matched (both arms `max_adapted_generations` 3).
- **Screen statistics and operating points.** AUC 0.9966 (CI 0.987–1.00), 53/53 flagged and 8/11 passed at 0.20 s, cutoff within ±0.2 s of onset in 47 of 53 (median +0.04 s), the 12 clearing clips' runs 0.44–1.04 s (9) / 2.4–3.12 s (3), pelvis rise 0.018–0.264 m, ballistic ratio 1.3–15.6 — all in `/home/linjiw/scene2motion/outputs/analysis_trackability_contract/receipt.json`.
- **Event timing.** Median frame 35 / 1.4 s, 40 of 49 inside 50 frames (Wilson 0.686–0.900), 4 after frame 60 — `/home/linjiw/scene2motion/outputs/analysis_event_frames/receipt.json` `summary.exp021.lifting_all.A_root_crossing`.
- **The two zeros.** Guarded endpoint 0/64 at every height and the unguarded 43/64 contrast (§III-C) both come from `/home/linjiw/scene2motion/outputs/exp022_exact_tracking_bridge/receipt.json` `summary.achieved.staged`.
- **Other channels.** Position-channel overshoot 1.68–2.32× (`exp1c_stepover/receipt.json` `per_amplitude.*.overshoot_x`); delayed prompt 3/16 vs 6/8, none at the predicted centre (`exp023b_prompt_switch_control/receipt.json`, `docs/figures/fig2_channel_funnel_numbers.json` `cross_checks_against_receipts.exp023b`); free walk tallest cleared box 5 mm (`exp020_text_only_control/receipt.json` `summary.nominal.max_lift_height_m`).
- **§VI-F record counts.** 192 + 76 = 268 collision-free, 6 rejected, 26 refused, and "192 meeting the margin" is the right reading of `accepted` vs `accepted_margin` per `/home/linjiw/scene2motion/scene2motion/verify/loop.py:14` — `/home/linjiw/scene2motion/outputs/corpus_pilot_v2/receipt.json`.
- **Vocabulary.** No "certified/certificate", no "did not fall", no "trackability contract", no throughput number in the abstract; "fell" appears only as a genuinely assessed outcome-class column. §III-C's evidence-level table matches `/home/linjiw/scene2motion/docs/project-goal-2026-09-02.md` line for line, and the local-traversal/navigation separation (§III-B) is stated correctly.
